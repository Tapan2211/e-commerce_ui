// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#6feeb7', // Your desired primary color
        },
        secondary: {
            main: '#ff4081', // You can also define a secondary color if needed
        },
    },
});

export default theme;
